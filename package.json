{
  "name": "quickinvoice",
  "version": "1.0.0",
  "description": "Professional invoicing SaaS for freelancers",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "NODE_ENV=test node tests/payment-link.test.js && NODE_ENV=test node tests/auth.test.js && NODE_ENV=test node tests/invoice-limit.test.js && NODE_ENV=test node tests/billing-webhook.test.js && NODE_ENV=test node tests/annual-billing.test.js && NODE_ENV=test node tests/invoice-crud.test.js && NODE_ENV=test node tests/billing-settings.test.js && NODE_ENV=test node tests/dunning.test.js && NODE_ENV=test node tests/free-footer.test.js && NODE_ENV=test node tests/invoice-view-and-status.test.js && NODE_ENV=test node tests/error-paths.test.js && NODE_ENV=test node tests/webhook-outbound.test.js && NODE_ENV=test node tests/landing.test.js && NODE_ENV=test node tests/gap-coverage.test.js && NODE_ENV=test node tests/csrf.test.js && NODE_ENV=test node tests/edge-cases.test.js && NODE_ENV=test node tests/rate-limit.test.js && NODE_ENV=test node tests/security-headers.test.js && NODE_ENV=test node tests/email.test.js && NODE_ENV=test node tests/trial.test.js && NODE_ENV=test node tests/onboarding.test.js && NODE_ENV=test node tests/status-whitelist.test.js && NODE_ENV=test node tests/reminders.test.js && NODE_ENV=test node tests/plan-check-constraint.test.js && NODE_ENV=test node tests/checkout-and-webhook-url.test.js && NODE_ENV=test node tests/paid-notification.test.js && NODE_ENV=test node tests/checkout-promo-tax.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "connect-pg-simple": "^9.0.1",
    "dotenv": "^16.3.1",
    "ejs": "^5.0.2",
    "express": "^4.18.2",
    "express-rate-limit": "^7.5.1",
    "express-session": "^1.17.3",
    "express-validator": "^7.0.1",
    "helmet": "^8.1.0",
    "node-cron": "^4.2.1",
    "pg": "^8.11.3",
    "resend": "^6.12.2",
    "stripe": "^14.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
