<%- include('../partials/head') %>
<%- include('../partials/nav') %>

<div class="min-h-screen flex items-center justify-center py-12 px-4">
  <div class="w-full max-w-sm">
    <div class="text-center mb-8">
      <h1 class="text-2xl font-bold text-gray-900">Welcome back</h1>
      <p class="text-gray-500 mt-1 text-sm">Log in to your QuickInvoice account</p>
    </div>

    <% if (locals.flash) { %>
      <div class="mb-4 p-3 rounded-lg text-sm <%= flash.type === 'error' ? 'bg-red-50 text-red-700 border border-red-200' : 'bg-green-50 text-green-700 border border-green-200' %>">
        <%= flash.message %>
      </div>
    <% } %>

    <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-8">
      <form action="/auth/login" method="POST" class="space-y-4">
        <input type="hidden" name="_csrf" value="<%= locals.csrfToken || '' %>">
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
          <input type="email" name="email" required autocomplete="email"
            value="<%= locals.values ? values.email : '' %>"
            class="w-full border border-gray-300 rounded-lg px-3 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
        </div>
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-1">Password</label>
          <input type="password" name="password" required autocomplete="current-password"
            class="w-full border border-gray-300 rounded-lg px-3 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
        </div>
        <button type="submit" class="w-full bg-brand-600 text-white font-semibold py-2.5 rounded-lg hover:bg-brand-700 transition">
          Log in &rarr;
        </button>
      </form>
    </div>

    <p class="text-center text-sm text-gray-500 mt-6">
      No account? <a href="/auth/register" class="text-brand-600 font-medium hover:underline">Sign up free</a>
    </p>
    <p class="text-center text-xs text-gray-400 mt-2">
      Forgot your password? Email <a href="mailto:support@quickinvoice.io?subject=Password%20reset%20request" class="underline hover:text-gray-600">support@quickinvoice.io</a> &mdash; we usually reset within a few hours.
    </p>
  </div>
</div>

</body>
</html>
