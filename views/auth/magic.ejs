<%- include('../partials/head') %>
<%- include('../partials/nav') %>

<div class="min-h-screen flex items-center justify-center py-12 px-4">
  <div class="w-full max-w-sm">
    <div class="text-center mb-8">
      <h1 class="text-2xl font-bold text-gray-900">Sign in with email</h1>
      <p class="text-gray-500 mt-1 text-sm">No password — we'll email you a one-tap sign-in link.</p>
    </div>

    <% if (locals.flash) { %>
      <div class="mb-4 p-3 rounded-lg text-sm <%= flash.type === 'error' ? 'bg-red-50 text-red-700 border border-red-200' : 'bg-green-50 text-green-700 border border-green-200' %>">
        <%= flash.message %>
      </div>
    <% } %>

    <% if (locals.invalid) { %>
      <div data-testid="magic-invalid" class="bg-white rounded-2xl shadow-sm border border-gray-200 p-8 text-center">
        <div class="text-3xl mb-2">⏳</div>
        <h2 class="text-lg font-semibold text-gray-900 mb-2">This link is no longer valid</h2>
        <p class="text-sm text-gray-600">
          Sign-in links expire after 30 minutes and can only be used once. Request a fresh one and try again.
        </p>
        <a href="/auth/magic" class="mt-4 inline-block bg-brand-600 text-white font-semibold px-4 py-2 rounded-lg text-sm hover:bg-brand-700 transition">
          Email me a new link &rarr;
        </a>
      </div>
    <% } else if (locals.sent) { %>
      <div data-testid="magic-success" class="bg-white rounded-2xl shadow-sm border border-gray-200 p-8 text-center">
        <div class="text-3xl mb-2">📬</div>
        <h2 class="text-lg font-semibold text-gray-900 mb-2">Check your inbox</h2>
        <p class="text-sm text-gray-600">
          If an account exists for <strong><%= locals.submittedEmail || '' %></strong>, we've sent a one-tap sign-in link. The link is good for 30 minutes.
        </p>
        <p class="text-xs text-gray-400 mt-4">
          Didn't get it? Check your spam folder, then <a href="/auth/magic" class="text-brand-600 hover:underline">request another link</a>.
        </p>
      </div>
    <% } else { %>
      <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-8">
        <form action="/auth/magic" method="POST" class="space-y-4" data-testid="magic-form">
          <input type="hidden" name="_csrf" value="<%= locals.csrfToken || '' %>">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
            <input type="email" name="email" required autocomplete="email"
              value="<%= locals.values ? values.email : '' %>"
              class="w-full border border-gray-300 rounded-lg px-3 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
          <button type="submit" class="w-full bg-brand-600 text-white font-semibold py-2.5 rounded-lg hover:bg-brand-700 transition">
            Email me a sign-in link &rarr;
          </button>
        </form>
      </div>
    <% } %>

    <p class="text-center text-sm text-gray-500 mt-6">
      Prefer a password? <a href="/auth/login" class="text-brand-600 font-medium hover:underline">Sign in with password</a>
    </p>
    <p class="text-center text-xs text-gray-400 mt-2">
      <a href="/auth/forgot" class="underline hover:text-gray-600">Forgot your password?</a>
    </p>
  </div>
</div>

<%- include('../partials/footer') %>

</body>
</html>
