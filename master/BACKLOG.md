# DecentInvoice — Backlog

> Each item advances a milestone in PLAN.md. Half-session-or-more of real engineering; no badge / copy-tweak / one-liner items.

_(empty — 2026-06-01 shipped owner `default_currency` → dashboard symbol propagation: 14 hardcoded `$` cells across all-time totals, recent-revenue card SSR + Alpine toggle re-render, invoice-table Amount column, pending-quick-invoice amount, and 8 prompt totals now read `<%= sym %>`. The previously shipped currency stack (form totals, invoice-view, invoice-print PDF, public share page, outbound emails) is now complete on the owner side — the dashboard was the last surface that contradicted the freelancer's currency choice. Next session derives a new milestone-advancing task or proposes the next Primary Objective.)_
